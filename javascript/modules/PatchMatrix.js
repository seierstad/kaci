/*global require, module */
"use strict";
var PatchMatrix = function (context, patch) {

    /* TODO: denne modulen bør inneholde  */

    function connect(Voice) {}

    function disconnect(Voice) {}

    /*
        eventlistener change.matrix.lfo[x].connect [inputnavn]
        eventlistener change.matrix.lfo[x].disconnect [inputnavn]
     */

};
module.exports = PatchMatrix;
